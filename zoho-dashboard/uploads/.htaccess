# Block direct access to metadata files
<FilesMatch "^_meta\.json$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Block PHP execution in this directory
<FilesMatch "\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
