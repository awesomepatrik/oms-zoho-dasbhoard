# Zoho credentials and tokens — NEVER commit these
zoho-dashboard-config/config.php
zoho-dashboard-config/tokens.json
zoho-dashboard-config/tokens.json.tmp

# API response cache
zoho-dashboard-config/cache/

# Locally cached contact attachment files (populated at runtime)
zoho-dashboard/uploads/

# Claude Code local settings
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
